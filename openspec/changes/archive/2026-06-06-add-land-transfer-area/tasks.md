## 1. 確認資料來源

- [x] 1.1 攔截一筆 detail API 的 JSON，找出「土地移轉面積」的確切欄位路徑（在 `d[]` 土地標示陣列：`d1`=地號、`d2`=土地移轉面積、`d3`=使用分區、`d5`=移轉持分）
- [x] 1.2 確認單位與格式（字串含「坪」如 `5.51坪`；多地號以「；」串接；無土地時留空）

## 2. 實作抓取

- [x] 2.1 在 `scraper.py` 的 `DETAIL_FIELDS` 加入「土地移轉面積」（置於陽台坪數之後）
- [x] 2.2 在 `enrich_with_details` 由 `j["d"][].d2` 取出該欄（缺值留空），並更新 docstring
- [x] 2.3 log 輸出加入土地移轉面積

## 3. 驗證

- [x] 3.1 重跑 `uv run scraper.py`（新東琚 105 筆），確認每筆共 29 欄且土地移轉面積 105/105 有值，欄位位於陽台坪數與車位類別之間
- [x] 3.2 與 detail JSON 的 `d2` 抽比對一致（如 `5.51坪`）
- [x] 3.3 封存本變更（`openspec archive add-land-transfer-area`），讓 `data-scraping` spec 反映 6→7 明細欄、28→29 欄
